import React from 'react';

const title = "HIOF";
const user = "Bruker Brukersen"


const Navbar = () => (
    <nav>
        <h2>{title}</h2>
        <p>{user}</p>
    </nav>
);

export default Navbar;